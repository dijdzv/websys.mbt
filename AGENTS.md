# 開発ルール

- 環境構築・利用方法・実行コマンドは README を正本とする。このファイルにはエージェントの作業上のルールを置く。
- `src/*.mbt` は自動生成物。変更は `generator/src/` に加え、再生成する。
- ブラウザ境界の値表現を変更した場合は、影響するバックエンドのブラウザテストで確認する。
- コードには How、テストには What、コミットには Why、コードコメントには Why Not を残す。
- 一時的な進捗や未完了タスクは Issue で管理し、このファイルへ追加しない。
