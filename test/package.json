{
  "name": "websys-test",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "moon build --target js",
    "test": "bun run build && bun run run.mjs",
    "install:browser": "bunx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
