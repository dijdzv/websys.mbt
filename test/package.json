{
  "name": "websys-test",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mise run build-test",
    "test": "mise run test",
    "install:browser": "mise run setup-test"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
